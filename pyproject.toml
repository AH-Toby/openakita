[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [
    ".packaging_test",
    ".git",
    "node_modules",
    "dist",
    "build",
    "__pycache__",
    "apps",
    "data",
    "docs",
    "scripts",
    "identity",
    "skills",
]
asyncio_mode = "auto"
addopts = "-p no:faker"
